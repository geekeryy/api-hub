Name: oms-api
Host: 0.0.0.0
Port: 8081
Log:
  Mode: console
  Level: info


Mysql:
  Username: ${MYSQL_USERNAME}
  Password: ${MYSQL_PASSWORD}
  Host: ${MYSQL_HOST}
  Port: ${MYSQL_PORT}
  Dbname: ${MYSQL_DBNAME}
Redis:
  Addr: ${REDIS_ADDR}
  Password: ${REDIS_PASSWORD}
  Db: ${REDIS_DB}

MemberService:
  Timeout: 10000
  Endpoints:
    - "user:8881"
    - "localhost:8881"

MailGun:
  Domain: 
  ApiKey: 
  Sender: 

Oms:
  OtpSecret: ${OMS_OTP_SECRET}
  PhoneList: ${OMS_PHONE_LIST}
  TokenExpire: ${OMS_TOKEN_EXPIRE}

Secret:
  RefreshToken: 
  PrivateKey: 
  PublicKey: 