Name: oms-api
Host: 0.0.0.0
Port: 8081
Timeout: 20000
Log:
  Mode: console
  Level: info

Mysql:
  Username: ${MYSQL_USERNAME}
  Password: ${MYSQL_PASSWORD}
  Host: ${MYSQL_HOST}
  Dbname: ${MYSQL_DBNAME}

Redis:
  Addr: ${REDIS_ADDR}
  Password: ${REDIS_PASSWORD}
  Db: ${REDIS_DB}

UserService:
  Timeout: 10000
  Endpoints:
    - ${USER_SERVICE_ENDPOINT}

MonitorService:
  Timeout: 10000
  Endpoints:
    - ${MONITOR_SERVICE_ENDPOINT}

MailGun:
  Domain: 
  ApiKey: 
  Sender: 

Oms:
  OtpSecret: ${OMS_OTP_SECRET}
  PhoneList: ${OMS_PHONE_LIST}
  TokenExpire: ${OMS_TOKEN_EXPIRE}

Secret:
  RefreshToken: 
  PrivateKey: 
  PublicKey: 